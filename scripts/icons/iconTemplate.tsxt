import { IconBox } from '../elements/IconBox'

import type { IconProps } from '../types'

export const /*ICON_NAME*/ = ({ color, ...nativeProps}: IconProps) => (
  <IconBox color={color}>
    /*ICON_SVG_SOURCE*/
  </IconBox>
)
